'use client';

export default function TicketCard({ticket, addQueue, inQueue}) {
    return (
        <div>
            <h1>HELLO WORLD</h1>
            <button>Add queue</button>
        </div>
    )
}